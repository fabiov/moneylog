<?php
/**
 * @var Zend\View\Renderer\PhpRenderer $this
 * @var \Accantona\Form\AccantonatoForm $form
 */
$chartData = array();
$morrisData = array();
?>
<style type="text/css">
    .has-error ul {
        color: #a94442;
        list-style-type: none;
        padding: 0;
    }
    #monthsFilter {
        margin: 8px 0;
    }
</style>
<?php echo $this->pageHeader("Movimenti $account->name"); ?>
<div class="row">
    <div class="col-lg-12">
        <div class="row">
            <?php echo $this->widgetText(['color' => 'green', 'col-lg' => '6', 'fa' => 'eur', 'label' => 'Saldo contabile', 'text' => $this->currencyForma($this->balanceAccount)]); ?>
            <?php echo $this->widgetText(['color' => 'green', 'col-lg' => '6', 'fa' => 'eur', 'label' => 'Saldo disponibile', 'text' => $this->currencyForma($this->balanceAvailable)]); ?>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="row">
                    <form>
                        <div class="col-lg-2 col-md-6">
                            <label>Dal</label>
                            <input class="form-control" name="dateMin" type="date" value="<?php echo $searchParams['dateMin']; ?>"/>
                        </div>
                        <div class="col-lg-2 col-md-6">
                            <label>Al</label>
                            <input class="form-control" name="dateMax" type="date" value="<?php echo $searchParams['dateMax']; ?>"/>
                        </div>
                        <div class="col-lg-3 col-md-6">
                            <label>Descrizione</label>
                            <input name="description" type="text" class="form-control" placeholder="Descrizione" value="<?php echo $searchParams['description']; ?>">
                        </div>
                        <div class="col-lg-3 col-md-6">
                            <label>Categoria</label>
                            <select class="form-control" name="category">
                                <option></option>
                                <?php foreach ($categories as $category): ?>
                                    <option value="<?php echo $category->id; ?>"<?php if ($category->id == $searchParams['category']) echo ' selected="selected"'?>>
                                        <?php echo $category->descrizione; ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-lg-2 col-md-12">
                            <label>&nbsp;</label>
                            <button type="submit" class="form-control btn btn-primary" value="cerca"><i class="fa fa-search"></i> Cerca</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body table-responsive">
                <table class="table table-striped table-bordered table-hover responsive" id="my-table">
                    <thead>
                        <tr>
                            <th>Data</th>
                            <th>Importo</th>
                            <th>Descrizione</th>
                            <th>Categoria</th>
                            <th>Azioni</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($rows as $row): ?>
                            <?php
                            $strDate = $row->date->format('Y-m-d');
                            if (isset($chartData[$strDate])) {
                                $chartData[$strDate] += $row->amount;
                            } else {
                                $chartData[$strDate] = $row->amount;
                            }
                            ?>
                        <tr>
                            <td class="date"><?php echo $this->dateForma($row->date); ?></td>
                            <td class="text-right amount"><?php echo $this->currencyForma($row->amount); ?></td>
                            <td class="description"><?php echo $row->description; ?></td>
                            <td class="description"><?php echo $row->category ? $row->category->descrizione : ''; ?></td>
                            <td class="text-right">
                                <a href="<?php echo $this->url('accantonaMoviment', ['action' => 'edit', 'id' => $row->id], ['query' => $searchParams]); ?>" class="btn btn-default" title="Modifica">
                                    <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                                </a>
                                <span data-href="<?php echo $this->url('accantonaMoviment', ['action' => 'delete', 'id' => $row->id], ['query' => $searchParams]); ?>" class="btn btn-default delete-item" title="Elimina">
                                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                                </span>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div id="morris" style="height: 188px;"></div>
        </div>
    </div>
</div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div id="balance-chart" style="height: 188px;"></div>
            </div>
        </div>
    </div>
<?php
// prepare data for barchar
foreach ($chartData as $strDate => $row) {
    $morrisData[] = array('x' => $strDate, 'y' => $row);
}
usort($morrisData, function ($a, $b) {
    if ($a['x'] == $b['x']) {
        return 0;
    }
    return $a['x'] < $b['x'] ? -1 : 1;
})
?>
<script type="text/javascript">
    $(document).on("click", ".delete-item", function(e) {
        var button = $(this);

        var html = '<h3>Cancellare l\'accantonamento selezionato?</h3>' +
            '<dl class="dl-horizontal">' +
            '<dt>Descrizione</dt><dd>'  + button.parents('tr').find('.description').html() + '</dd>' +
            '<dt>Importo</dt><dd>'  + button.parents('tr').find('.amount').html() + '</dd>' +
            '<dt>Data</dt><dd>'  + button.parents('tr').find('.date').html() + '</dd>' +
            '</dl>';
        bootbox.confirm(html, function(result) {
            if (result) {
                location.href =  button.data('href');
            }
        });
    });

    $(document).ready(function(){
        $('#my-table').DataTable({
            "columnDefs": [
                {"targets": 0, "type": 'date-eu'},      //data
                {"targets": 1, "type": 'currency-it'},  //importo
                {"orderable": false, "targets": -1}     //azioni
            ],
            "language":{
                "url": "/js/data-table-Italian.json"
            },
            "order": [
                [0, "desc"]
            ],
            "responsive": true,
            "searching": false
        });
    });
</script>
<?php $this->inlineScript()->captureStart(); ?>
Morris.Bar({
    element: 'morris',
    data: <?php echo json_encode($morrisData); ?>,
    xkey: 'x',
    ykeys: 'y',
    labels: ['Importo'],
    hideHover: "auto",
    resize: true,
    "xLabelFormat": function (x) {
        var d = x.label.split('-');
        return d[2] + '/' + d[1] + '/' + d[0];
    },
    "yLabelFormat": function (x) {
        var parts = x.toString().split(".");
        parts[0] = parts[0].replace(/\B(?=(\d{3})+(?!\d))/g, ".");
        return parts.join(",") + ' €';
    }
});
Morris.Line({
    "element": "balance-chart",
    "data": <?php echo json_encode($dataLineChart); ?>,
    "xkey": 'date',
    "ykeys": ['balance'],
    "labels": ['Saldo'],
    "xLabelFormat": function (x) {
        var d = x.getDate();
        var m = x.getMonth() + 1;
        var y = x.getFullYear();
        return (d > 9 ? d : '0' + d) + '/' + (m > 9 ? m : '0' + m) + '/' + y;
    },
    "yLabelFormat": function (x) {
        var parts = x.toString().split(".");
        parts[0] = parts[0].replace(/\B(?=(\d{3})+(?!\d))/g, ".");
        return parts.join(",") + ' €';
    }
});
<?php $this->inlineScript()->captureEnd();?>
<?php
$i = '<i class="mfb-component__child-icon glyphicon glyphicon-equalizer"></i>';
$balanceHref = $this->url('accantonaAccount', array('action' => 'balance', 'id' => $account->id));
$this->floatingButtons()
    ->addRawItem("<span data-href=\"$balanceHref\" data-mfb-label=\"Conguaglia\" class=\"balance mfb-component__button--child\">$i</span>")
    ->addAnchorItem(array('href' => $this->url('accantonaMoviment', array('action' => 'add', 'id' => $account->id)), 'label' => 'Nuovo movimento', 'icon' => 'plus'))
    ->addAnchorItem(array('href' => $this->url('accantonaMoviment', array('action' => 'move', 'id' => $account->id)), 'label' => 'Giroconto', 'icon' => 'transfer'));

$this->bindBalance('.balance');